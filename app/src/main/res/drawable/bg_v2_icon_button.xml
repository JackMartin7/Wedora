<?xml version="1.0" encoding="utf-8"?>
<!-- Circular icon button on the page rather than on a photo — Explore's
     filter button. Glass fill plus hairline, matching the search field. -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="oval">
    <solid android:color="@color/wedora_glass" />
    <stroke android:width="1dp" android:color="@color/wedora_hairline" />
</shape>
