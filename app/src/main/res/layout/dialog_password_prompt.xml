<?xml version="1.0" encoding="utf-8"?>
<!--
    Password input for the re-authentication dialogs (change email, delete
    account). Firebase requires a recent sign-in for both, and neither has a
    password field of its own on the screen.
-->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:paddingHorizontal="24dp"
    android:paddingTop="8dp">

    <EditText
        android:id="@+id/etDialogPassword"
        style="@style/WedoraInputField"
        android:layout_width="match_parent"
        android:layout_height="48dp"
        android:hint="@string/hint_current_password"
        android:inputType="textPassword"
        android:imeOptions="actionDone"
        android:autofillHints="password" />

</LinearLayout>
